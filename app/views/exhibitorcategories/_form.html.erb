<%= form_for(@exhibitorcategory) do |f| %>
  <% if @exhibitorcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exhibitorcategory.errors.count, "error") %> prohibited this exhibitorcategory from being saved:</h2>

      <ul>
      <% @exhibitorcategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
